// shadcn/ui canonical utility — re-exports from the existing cn helper so both
// `@/lib/utils` (shadcn convention) and `../lib/cn` (project convention) resolve identically.
export { cn } from "./cn";
