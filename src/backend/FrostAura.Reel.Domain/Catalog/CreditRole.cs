namespace FrostAura.Reel.Domain.Catalog;

/// <summary>The three credit roles that feed affinity features. Other crew is noise for taste.</summary>
public enum CreditRole
{
    Actor,
    Director,
    Writer,
}
